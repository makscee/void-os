// catalog.ts — list installable skills from the vendored catalog (Task 4)
export interface CatalogSkill { dir: string; name: string; description: string; }
export function listCatalogSkills(_catalogRoot: string): CatalogSkill[] { throw new Error("not implemented"); }
