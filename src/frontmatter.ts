// frontmatter.ts — parse SKILL.md front matter (Task 3)
export interface SkillMeta { name: string; description: string; }
export function parseFrontmatter(_md: string): SkillMeta { throw new Error("not implemented"); }
